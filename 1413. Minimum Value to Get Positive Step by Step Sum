class Solution {
public:
    int minStartValue(vector<int>& nums) {
        int num =0 ;int sum =0;
        for( int i=0;i<nums.size();i++ ){
            sum+=nums[i];
            num=min(num,sum);

        }return 1- num;
        
    }
};
